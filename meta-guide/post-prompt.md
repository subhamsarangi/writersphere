create a minimal page for writing a article/essay/note (every-10-second auto-saving markdown editor with toggle theme button to preview it). its default status is draft (other statuses are published, unpublished, archived, deleted). selection of a category and/or subcategory is important [tables are created already for them], smooth ux of tags also important [table not created]. minimum of 5 tags must. important datetimes should also be included in the table. you may use new packages packages. since im using supabase, the rows would have rls.